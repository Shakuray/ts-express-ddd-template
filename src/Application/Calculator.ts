export default class Calculator {
    public rest(a: number, b: number): number {
        return a - b;
    }
    public sum(a: number, b: number): number {
        //Extra complex function
        return a + b;
    }
}
